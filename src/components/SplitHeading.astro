---
interface Props {
  as?: "h1" | "h2" | "h3";
  class?: string;
}

const { as: Tag = "h2", class: className = "" } = Astro.props;
---

<Tag class={className}>
  <span data-split-source set:text={await Astro.slots.render("default")} />
</Tag>
